[Settings]
;Section 8: Prejudice news payload. Served UTF-16LE by server/news.py; the game cannot read it as ASCII.
;
;Line format:  <GameInfoFilter>-<Class>.<Property>=<Value>
;  GameInfoFilter  class the live GameInfo must derive from, or the line is skipped silently. News is
;                  applied during login, while S9GameInfoEntryEmpty is live, and that does NOT derive
;                  from S9GameInfo. Use TGGameInfo, their common ancestor.
;  Class           bare class name. Package-qualifying it (S9Game.S9GameInfoAssault) breaks the split
;                  on '.' and rejects the whole section.
;  ';'             comment, honoured in this section only. A malformed line aborts the rest of the
;                  section, so change one thing per line.
;
;The two lines below clear the unlock gate on Assault and Skirmish. Without them a dedicated server
;hosting either mode exits during startup with "This game mode is not available."
;Full reference: docs/game_modes_and_news.md
TGGameInfo-S9GameInfoAssault.Unlock=None
TGGameInfo-S9GameInfoArcade.Unlock=None

;The in-game banner follows. WARNING: the section below is scanned with NO comment handling and NO
;terminator. The first line holding the key wins, and everything right of its first '=' becomes the
;banner. So no line after the header may mention that key, not even inside a comment. Keep the block
;to exactly one line.
[MOTD]
MOTD_INT=Welcome to the Section 8 Prejudice revival server. Assault and Skirmish are unlocked.
